{
  "description": "async arrow with 3 args threads all arguments through the coroutine",
  "expect": {
    "parse": true,
    "compile": true,
    "run": true,
    "stdout": "10",
    "exitCode": 0
  },
  "knownFailure": "Pre-existing failure - not in current task scope"
}
