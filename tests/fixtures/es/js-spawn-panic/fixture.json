{
  "description": "[dialect] uncaught throw in js-spawned coroutine panics: exit 1, diagnostic on stderr (golden). The panic line moved stdout->stderr in v0.2.6 to match the main uncaught path (_uncaught_report); the fixture runner only captures stdout, so the message text is asserted by the repro recorded in docs/progress, not here.",
  "expect": {
    "parse": true,
    "compile": true,
    "run": true,
    "stdout": "main-ran",
    "exitCode": 1
  }
}
