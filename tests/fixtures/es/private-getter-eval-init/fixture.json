{
  "description": "Direct eval in an instance field initializer can read a private getter already installed on the instance",
  "expect": {
    "parse": true,
    "compile": true,
    "run": true,
    "stdout": "Test262",
    "exitCode": 0
  }
}
