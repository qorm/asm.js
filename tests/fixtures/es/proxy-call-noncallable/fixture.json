{
  "description": "Calling a Proxy whose target is not callable must TypeError, not synthesize [[Call]] and SIGBUS.",
  "expect": {
    "parse": true,
    "compile": true,
    "run": true,
    "stdout": "ok",
    "exitCode": 0
  }
}
