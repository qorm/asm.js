{
  "description": "x64 store(S5,n,V0) must not alias RAX: Map insertion-order list stays live. x64 _ta_is_oob must not clobber A2: RAB TypedArray fill writes index 0. x64 shrImm(V0,RET,48) must not collapse a species ctor to tag 0x7FFF.",
  "expect": {
    "parse": true,
    "compile": true,
    "run": true,
    "stdout": "ab\n2\n1,1,1,1\n1,1,1,1\n2,1,3",
    "exitCode": 0
  }
}
