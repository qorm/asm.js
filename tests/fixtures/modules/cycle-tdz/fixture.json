{
  "description": "cyclic const import should still preserve TDZ semantics (catchable ReferenceError; uncaught at top level → stderr + exit 1)",
  "entry": "a.js",
  "expect": {
    "parse": true,
    "compile": true,
    "run": true,
    "stdout": "",
    "exitCode": 1,
    "stderr": "Uncaught ReferenceError: Cannot access 'a' before initialization"
  }
}
