{
  "description": "node:net connect options: custom lookup, lookup/ready events, listen({port,host}), noDelay/keepAlive options",
  "timeoutMs": 8000,
  "expect": {
    "parse": true,
    "compile": true,
    "run": true,
    "stdout": "lookup true 127.0.0.1 4 localhost\nready true\nlookup-arg localhost:4\nopts",
    "exitCode": 0
  }
}
